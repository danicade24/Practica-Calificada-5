from flask import Flask
app = Flask(__name__)

@app.route("/")
def hello():
    return "Hola mundo!!! \n Se esta  usando el puerto {{ flask_port }}"

if __name__ == "__main__":
    app.run(host='0.0.0.0', port = {{ flask_port }})